{
  "watch": ["src", "config.schema.json"],
  "ext": "ts",
  "ignore": [],
  "exec": "npm run build && homebridge -U ./dev -D",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings"
  }
}
